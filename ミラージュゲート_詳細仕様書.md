# ミラージュゲート（MIRAGE GATE）詳細仕様書

> 本書は実装（`monster_gate.html`）の現行挙動に基づく詳細仕様。要件定義書（企画意図）／Unity設計図（移植向け）／桜井監修レポート（演出方針）を補完する「いま動いているものの仕様書」。
> 最終更新: 2026-06-20

---

## 1. 概要・コンセプト

- **ジャンル**: 1画面・グリッド制ローグライト・ダンジョンRPG（ターン制／見下ろし型）。
- **モチーフ**: メダルゲーム風の「賭けて潜って稼ぐ」サイクル ＋ カードによるアビリティ運用。
- **核となる遊び**: 「1対1のお膳立て（通路に誘い込む・空振りで引き寄せる）」「手札カードの使いどころ」「育成上限を意識した格上狩り」「撤退かクリアかの判断」。
- **デザイン指針**: プレイヤーファースト。リアルさより「伝わる・気持ちいい」。手応えの積層演出（ヒットストップ＋画面揺れ＋効果音）を重視。著作権配慮（既存IP不使用）。

## 2. 基本情報・技術構成

| 項目 | 内容 |
|---|---|
| 実体 | 単一HTMLファイル `monster_gate.html`（HTML+CSS+JS、外部フレームワーク無し） |
| 描画 | `<canvas>` 2D。タイル描画＋スプライト＋手続き的エフェクト |
| 音 | Web Audio API による手続き合成（効果音・BGM。音声ファイル不使用） |
| 保存 | `localStorage`（所持メダル・コレクション・進行など） |
| 画像素材 | `assets/`（AI生成のヒーロー/モンスター立ち絵・8方向スプライト・カード絵・背景・NPC 等） |
| 状態管理 | グローバル `G` オブジェクト（後述） |

## 3. 目的とコアループ

1. **メダルを賭けて**ダンジョンへ出撃（`bet`）。
2. ダンジョンを探索：敵を倒し、**宝石(WIN)**・カード・メダルを集めながら最深部を目指す。
3. **クリスタル（ゴール）**に触れてクリア → 配当（`win`）と持ち帰り。
4. または途中で**撤退（エスケープ）**して、それまでの宝石・カードを持ち帰る。
5. **死亡**すると手札カードと賭けメダルを失う（コレクション保有分は安全）。
6. 稼いだメダル・カードで次の出撃を強化 → 繰り返し。

「動く≠遊べる」を重視し、各状態のボタン操作・ソフトロック無し・画面内収まりをQAする方針。

## 4. 操作方法

- **移動/攻撃**: テンキー／矢印に相当する8方向（`DIRS` 1–9、5は待機/その場斬り）。
  進行方向に敵がいれば攻撃、無ければ移動。
- **その場斬り（5/待機）**: 向いている方向へ空振り斬撃。**空振りは敵を1マス引き寄せる**（お膳立て）。
- **カード**: 「🎴 カード一覧」ボタンで手札を画面表示 → カードをクリックで使用。各カードに「→MP+」ボタンでMP変換も可能。
- 単体対象カードは**最寄りの見えている敵へ自動照準**（攻撃魔法は後述の向き直線優先）。

## 5. 移動・ターン制の仕様

- マップは矩形グリッド（`G.map`：0=壁, 1=部屋床, 2=通路床）。プレイヤーが1手動くと敵フェイズが回る完全ターン制。
- **角抜け禁止（No corner-cutting）**: 斜め移動・斜め攻撃は、両隣の直交マスが共に壁でないことが条件。
  → **通路（1マス幅）では斜めに動けず・斜めに斬れず、正面1対1のファネリングが成立**（敵の斜め攻撃も同様に通らない）。
- **氷結床**: 入力方向へ滑る（停止まで1ターン）。
- 部屋に初めて入った時のみ小回復（各部屋1回・通路では回復しない）。
- カメラはプレイヤー追従＋マップ端クランプ。視覚位置（`_vx/_vy`）を滑らかに補間。

## 6. 戦闘システム

- **通常攻撃ダメージ** = `(攻撃力+剣ATK)×剣倍率 − 敵守備 + 乱数(-2〜+2)`（最低1）。
- **会心の一撃**: 職業別の会心率（既定12%、幸運職18%、侍22% 等）で1.5倍。金色斬撃＋大数字＋画面揺れ＋長めヒットストップ。
- **格下撃破補正**: 自レベルが敵の育成上限`cap`以上の通常敵は1撃（ボス/ガーディアン/カオス/シャドーは除外）。
- **攻撃モーション**: 8方向対応で複数パターン（弧斬り/突き/横薙ぎ/二段斬り/振り下ろし）。踏み込み(lunge)＋着弾同期の被弾揺れ。
- **手応え演出（積層）**: 命中の瞬間にヒットストップ（通常120/会心260ms目安）＋画面揺れ（タメ→減衰）＋素材別効果音。撃破時はボス±10px/雑魚±5pxで段階化。
- **ドレインソード**（装備）: 与ダメの**1/8を吸収（1回最大4・HP満タン時は回復しない）**。※強すぎたため弱体化済み。

## 7. 成長システム（育成上限）

- 各モンスター型に `cap`（3職系統ぶんの配列、`capIdx`で参照）。**自分が倒した敵型のcap最大値**が、そのランでのレベル上限になる。
- 弱い敵だけ倒しても上限が上がらない＝**格上を倒すほど伸びる**設計（モンスターゲート式）。
- 撃破時、`cap−自Lv`の差が大きいほど高確率・大きく成長（最大+5）。上限到達で停止。

## 8. 職業（プレイアブル17＋隠しレン）

ステータス: HP / MP / ATK / DEF / 視界 / 系統(capIdx 0=物理,1=魔法,2=幸運) / 補正（pot=回復, mag=魔法）。

| 職業 | HP | MP | ATK | DEF | 視界 | 系統 | 特徴 |
|---|---|---|---|---|---|---|---|
| 戦士 warrior | 50 | 20 | 12 | 6 | 3 | 物理 | 安定・初心者向け |
| 魔法使い mage | 42 | 40 | 8 | 4 | 4 | 魔法 | 回復+25%/魔法+50%・広視界 |
| ギャンブラー gambler | 34 | 26 | 9 | 3 | 3 | 幸運 | 稼ぎ倍率UP・低耐久 |
| 侍 samurai | 44 | 22 | 14 | 5 | 3 | 物理 | 会心22%の手数型 |
| 竜騎士 dragoon | 58 | 24 | 11 | 9 | 3 | 物理 | 高HP/防御の前線 |
| 聖騎士 paladin | 56 | 22 | 14 | 9 | 3 | 物理 | 高耐久＋回復+25% |
| 魔導士 sorcerer | 36 | 38 | 9 | 4 | 3 | 魔法 | MP/魔法が非常に高い後衛 |
| 闇騎士 darkknight | 56 | 24 | 16 | 5 | 3 | 物理 | 高火力・守備薄の諸刃 |
| ゼラート fallen | 48 | 32 | 14 | 6 | 4 | 魔法 | 魔法・視界に優れる遊撃 |
| ヴァルキリー valkyrie | 54 | 28 | 14 | 7 | 4 | 物理 | 広視界の遊撃騎士 |
| 魔剣姫 magicblade | 46 | 36 | 13 | 6 | 3 | 魔法 | 剣技＋魔法の両刀 |
| 紅竜騎士 reddragoon | 60 | 24 | 13 | 10 | 2 | 物理 | 全職最高の耐久・鈍重で狭視界 |
| ギルガメシュ gilgamesh | 52 | 26 | 17 | 5 | 3 | 物理 | ガラスの大砲・配当UP |
| ルシファー lucifer | 50 | 34 | 14 | 6 | 4 | 魔法 | 高魔法・広視界 |
| オーディン odin | 44 | 36 | 11 | 5 | 5 | 魔法 | 最広視界5・最高魔法 |
| ロキ loki | 42 | 30 | 12 | 4 | 4 | 幸運 | 会心・幸運・視界の技巧派 |
| （隠し）主人公レン ren | 46 | 23 | 10 | 5 | 3 | 物理 | 剣＋魔の中間。MPはHP半分。職業選択非表示 |

※ `crit`（会心率）, `luck`（配当）, `potBonus`/`magBonus` が職ごとに個別設定。

## 9. カードシステム

- **カテゴリ**: 回復(heal) / 攻撃魔法(mag) / 補助・状態異常(sup) / 装備(equip)。
- **レアリティ**: R < SR < SSR < UR < LR（`cardRarity`：コストと禁忌で判定。コスト24/44/70以上で SR/SSR/UR、禁忌は UR、オーブコールは LR）。レア度はホロ/箔の見た目に反映。
- **MP**: 使用にMP消費（装備はレア度比例 R6/SR10/SSR14/UR18/LR18）。不要カードは行動を消費せず**MPに変換**可能。
- **手札と袋**: 出撃時に「袋(bag)」から手札(hand)へ。手札は**死亡で消失・撤退/クリアで保持**。持ち込み上限はダンジョン依存（幻は5枚）。
- **装備は剣1/盾1スロット**: 新しい剣を装備すると前の剣効果（吸収/金/睡眠/倍率/攻撃）をリセット。指輪は複数重複可（このランのみ）。

### 主要カード効果（抜粋）
- **ファイヤー/メテオ/サンダー（攻撃魔法）**: **向いている直線上の敵を最優先で狙う**（直線上に居なければ最寄りの見えている敵）。職の`magBonus`と魔法指輪で威力補正。
- **スピア**: 直線を貫通して複数体ヒット。
- **デス**: 対象を即死。代償でLv/最大HP/ATK低下。
- **チャーム（誘惑）**: 単体を一定ターン味方化。**ボス・ガーディアン・オーブ・城主・カオスには無効**（ボスをチャームしてクリスタルが出ず詰む事故を防止）。
- **ポーション類**: HP回復。**回復音とともに青い回復オーラがプレイヤーを包む**演出。
- **ドレインソード/ゴールドソード/スリープソード/各種ソード・盾・指輪・ポケット**: 装備系。
- 補助: ロック/スリープ/スロウ/パニック/ストーム/ブライト/マップ/サーチ/サイレント/テレポート/エスケープ/バリア/インビジブル/コンボ/スピード/パワーアップ 等。

## 10. モンスター

- 共通フィールド: `hp, atk, def, role, cap[3], pay（配当/報酬規模）, emoji, color`。
- **role（行動AI）一覧**: melee（接近）, revive（復活）, swarm/summon（増殖）, lunge（1マス跳躍：通路では不可）, charge（直線突進：マッドブル）, ranged（8方向ライン射撃：物陰には撃たない）, healer（味方回復・距離取り）, coward（臆病）, knock（吹き飛ばし）, poison（毒床を残す）, magicimmune（魔法無効：クリスタルゴーレム）等。
- **索敵**: 視界内・同部屋・近距離・直線視線で「ロックオン」、以後追跡。透明化中は見失う。
- **撃破**で一定確率に**カード化（勧誘）**、育成上限の更新、ゴールドソード配当等。

## 11. 仲間システム（チャーム/勧誘 → 召喚 → 同行 → 戦闘 → 再カード化）

- 入手: チャームで一時味方化、または撃破時の勧誘で**モンスターカード**化（`G.monCards`）。
- 召喚: モンスターカードから盤面に同行仲間として出す。
- **追従AI（`allyAct`）**:
  1. 隣接する敵がいれば攻撃。
  2. **一定距離（6マス）内に敵が居れば最優先で倒しに行く**（自分かプレイヤーいずれかから6マス）。
  3. 敵が居なければ**プレイヤーの「後ろ」（進行方向の反対マス）について行く**。隣接張り付き時は待機。
- **進路は塞がない**: プレイヤーが進む先に味方が居たら入れ替わって道を譲る。

## 12. ボスとクリア

- 各章/ダンジョン最深部に**章ボス・守護者**を配置。**倒すまでクリアのクリスタルは出さない**（全ダンジョン共通）。
- 最終章は**幻影のオーブ**（HP2500）。撃破でクリスタル出現 → 触れてクリア。
- 城主撃破は敗北カットシーン → 仲間入りの演出。

## 13. ダンジョン一覧

`star`=難度, `floors`=階数, `bet`=賭けメダル, `win`=配当, `size`=マップ。

| キー | 名称 | ★ | 階 | BET | WIN | 備考 |
|---|---|---|---|---|---|---|
| tutorial | 操作の間 | 0 | 1 | 0 | 30 | チュートリアル |
| hajimete | はじめての穴 | 1 | 3 | 3 | 40 | 基本練習 |
| easy | やさしい谷 | 1 | 3 | 5 | 60 | 1対1練習 |
| kiraku | きらくな小径 | 2 | 5 | 7 | 90 | 挟まれの怖さ |
| futsuu | ふつうの坑道 | 2 | 5 | 10 | 160 | 宝石が落ち始める |
| fun | うれしい森 | 3 | 4 | 12 | 140 | 稼ぎ・カード集め |
| tegowai | てごわい岩窟 | 4 | 5 | 15 | 320 | 部屋多・5Fに死神 |
| fierce | はげしい洞窟 | 5 | 4 | 18 | 260 | ジェネレーター多発 |
| hard | 難しい遺跡 | 6 | 7 | 25 | 400 | 広大・最上位級 |
| maboroshi | 幻 | 7 | 8 | 0 | 1200 | 隠し・持込5枚・一度きり |

- 章/ストーリー/ワールド（塔=上って進む `world`）モードも存在。塔は階段の上下方向を反転。

## 14. フロアギミック・障害物

- **床ギミック**: 氷結（滑る・ダメージ無）/ 毒沼 / 溶岩 / 呪印（暗転）。
- **障害物**: 岩・石柱・魔石・クリスタル群（高難度で華やかな発光）。斜め移動は岩等の障害物は通すが、角の「壁」は通さない。
- **ジェネレーター**: 煙の前兆 → 敵が湧く。サイレントで停止可。

## 15. 経済・施設

- 通貨: **メダル**（賭け/配当）, **宝石(WIN)**（床ドロップの配当）。
- **ショップ**: ランダム10種を各1枚販売（売切表示・購入確認モーダル・店主のセリフが装備/手札数/入荷で変化）。街ショップはランごと再入荷。
- **スロット**: 台ごとに掛金/レア率（item/gambler/卵）。卵スロットは高レア寄り（45%でUR級）。当たりに賞賛演出＋専用SE。
- ショップ/スロットは**売店の部屋・スロットコーナー**としてフロアに出現（★3+で確率配置）。

## 16. ビジュアル／アニメーション仕様

### 8方向スプライト
- ヒーロー/主要モンスターは `t_d_<キャラ>_<ビュー>_<状態>.png`。
- **ビュー**: front / back / side / fdiag（斜め前）/ bdiag（斜め後ろ）。横/斜めは右向き基準で**左は反転**。8方向は「縦成分でfront/back、横成分はside＋反転、斜めはfdiag/bdiag」で表現。
- **状態**: idle / walk / hit / attack / atkwind ほか。

### 滑らかな歩行（多コマ・HF生成）
- 各職・各ビューに `walk1 / walk2 / walk3` を用意し、移動中は **[walk1→walk2→walk3→walk2] を120msで連続ループ**（idleを挟まないので滑らか）。
- 生成が無いキャラ（モンスター等）は従来の idle/walk 2コマトグルにフォールバック。
- **8方向フル対応**（front/back/side/fdiag/bdiag すべてに walk1-3 を生成）。

### 多コマ攻撃（HF生成）
- 各職・各ビューに `atk1（振りかぶり）→ atk2（振り抜き）→ atk3（追い打ち/フォロースルー）` を用意。lunge中に3分割で再生。
- 未生成は atkwind→attack→idle にフォールバック。

### 装備モーション（剣を掲げる／天高く掲げる）
- 装備カード使用時、主人公が **`raisemid → raise`（剣を頭上へ掲げきり、金色に発光）** の専用2コマポーズ＋本体の伸び上がり＋頭上の光るアイコン/光輪/きらめき。
- 生成が無い職は実 `atkwind`（剣を上げたポーズ）にフォールバック。
- **レアカード（SR以上）使用時**は、使用カードを画面に大きく出す賞賛演出＋画面きらめき。

### HF（Hugging Face）忠実生成パイプライン
- 既存のダンジョンスプライト（`*_idle`）を入力に、**FLUX.1-Kontext**（キャラ・絵柄を保持してポーズだけ編集）で walk/attack/raise を**逸脱なく**生成 → 白背景を透過に戻す。
- 生成物は公開データセット `takaokkkk/mirage-hero-anim` に格納し、`assets/` へ取り込み。

## 17. 演出（手応え）

- **ヒットストップ**: 命中/撃破の瞬間に時を止める（`now()`はヒットストップ中フリーズ）。
- **画面揺れ**: 実時間基準でタメ（フル振幅）→余韻（減衰）。威力で持続が変化。
- **オーラ/バフ**: 回復=青、強化=金/青、状態異常=紫の波、装備=金のきらめき＋チャイム。
- **撃破**: 4コマ死亡アニメ（対応キャラ）＋煙＋振幅段階化（ボスは派手）。

## 18. オーディオ

- すべて Web Audio のオシレータ／ノイズで手続き合成（`tone`/`nzHit`/`thump` 等）。
- 効果音: 斬撃・命中（包丁×まな板ASMR志向）・会心・撃破・遠隔着弾・装備チャイム・UI・宝石・レア入手（高音）等。
- BGM: 通常／ボス（ボス撃破で通常へ戻る）。テキスト送り速度などの演出同期。

## 19. セーブ

- `localStorage` に所持メダル・コレクション・モンスターカード・進行・オプションを保存（`saveGame`）。
- 出撃時に袋確定・セーブ。死亡で手札消失、撤退/クリアで保持。

## 20. オプション／アクセシビリティ

- テキスト速度（`G.opt.textSpeed`）、確認ダイアログ省略（`noConfirm`）、向きインジケータ（faceMode/diagMode）等。

## 21. 主要データ構造（グローバル `G` 抜粋）

```
G = {
  job, dungeon, floor, hp/maxhp, mp/maxmp, atk, def, lvl, _cap,
  px, py, _vx, _vy, _dirX, _dirY, _face,      // 位置・向き・視覚補間
  map, seen, lit, rooms, tfx, obst, w, h,     // マップ/視界/ギミック/障害物
  monsters[], allies[], items[], gem, stairs, upstairs,
  hand[], bag[], collection{}, monCards{}, medals, loot,
  swordAtk, shieldDef, swordMul, goldSword, drain, sleepSword, equip{}, rings{},
  status{barrier,invis,combo,speed,regen,bright,silent,...},
  busy, over, _lockUntil, _lunge, _hit, _shake, _equipPose, ...
}
```

## 22. このセッションの更新履歴（2026-06-20）

- **戦闘/挙動**: ①ボスはチャーム不可 ②③仲間AI（近接敵を最優先→居なければ後ろ追従・進路は塞がない）④通路で斜め斬り禁止（攻撃にも角抜け適用）⑤ドレインソード弱体化（吸収1/8・最大4・満タン時なし）⑦攻撃魔法は向き直線上の敵を優先。
- **演出**: ⑥レアカード使用演出＋全17職の「剣を掲げる」装備モーション。⑧⑨ポーション回復オーラ＋音／カードボタンで所持カード一覧（既存確認）。
- **アニメ（HF Kontextで実スプライトから忠実生成）**: 全17職の **8方向 滑らか歩行（walk1-3）**、**多コマ攻撃（atk1-3）**、**剣を掲げる（front raise/raisemid）**。
- **QA**: プレビューで機能スモークテスト（仲間AI engage/follow・魔法直線・通路斜め斬り禁止）すべてPASS、コンソールエラー0。

## 23. ファイル構成・素材

```
ミラージュゲート桜井/
├ monster_gate.html              ← 本体（＋ .bak_* 多数のバックアップ）
├ ミラージュゲート_要件定義書.md   ← 企画・要件
├ ミラージュゲート_Unity設計図.md  ← Unity移植向け設計
├ 桜井監修レポート.md             ← 監修＋演出方針＋実施ログ
├ ミラージュゲート_詳細仕様書.md   ← 本書
├ assets/                        ← 画像（ヒーロー/モンスター8方向スプライト・カード絵・背景・NPC 等）
└ _hf_tools/                     ← HF素材生成スクリプト（アップロード／Kontext生成）
```
- 関連HFデータセット: `takaokkkk/mirage-hero-anim`（アニメ素材）, `mirage-vn`/`mirage-opening`/`mirage-story2`/`mirage-npc`（背景・立ち絵・NPC）。

## 24. 既知の制約・今後

- 正面歩行はモデルの特性上「脚運び＋上下動」中心で武器位置が微妙に揺れる場合がある（横/斜めは脚の運びが明確）。
- 一部生成フレームで装備の細部が変化することがある（例：杖が剣寄りに）→ 必要に応じてリファイン。
- 「剣を掲げる」装備モーションは現状 front のみ（side/back/斜めへの展開は任意の拡張候補）。
- 最終的な手触りQA（押せるボタン・ソフトロック無し・テンポ）は実機で確認する方針。

---
*本書はコード現行挙動に基づく。数値・確率はバランス調整で変動しうるため、最終値はコード（`JOBS`/`DUNGEONS`/`MONSTERS`/`CARDS`）を参照。*

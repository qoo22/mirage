
### ⑨忠実HF生成＝完了（2026-06-20）
- `_hf_tools/upload_hero_sprites.py` で全20職業 front/side idle を `takaokkkk/mirage-hero-anim/src/` へUP。
- `_hf_tools/kontext_batch.py`(hf_jobs uvにインライン投入) で FLUX.1-Kontext により各職業の `front_raisemid`/`front_raise`(剣を掲げる)を**忠実生成**＋白背景を透過化。20職業ALL DONE。
- 生成物を `assets/t_d_<job>_front_raise(mid).png`(計40) にDL済み。`dset.mk` に raise/raisemid 追加、`dirSprite` の装備分岐を raisemid→raise の2コマに変更（未生成職はatkwind→attackへフォールバック）。プレビューで20職分の raise 画像ロード＆ポーズ解決を確認・コンソールエラー0。
- バックアップ: `monster_gate.html.bak_equip_raise`。
- ⚠️ユーザーがHF Writeトークンをチャットに貼付＝**使用後に https://huggingface.co/settings/tokens で失効推奨**（このトークンはローカルのhfキャッシュにのみ使用、コードには未保存）。

# ミラージュゲート 個体化・スキン経済 設計提案

> モンスターカード（仲間）と装備の剣を「世界に1つの個体」にし、**ランダム生成 × レアパターン × 変異 × 来歴 × 見せびらかし × 市場 × 供給制限**を全部揃える設計。
> ポケモン型(個体差)＋PoE型(性能ランダム)＋CS2型(見た目レア)＋Brainrot型(変異) のハイブリッド。
> 受領: 2026-06-20。実装ステータスは末尾「実装状況」参照。

## 0. 最重要の前提
- **希少性はlocalStorageでは絶対に成立しない**。クライアント保存＝改ざん可能＝供給無限＝価値ゼロ。本格RMTを目指すなら**サーバー権威型の個体台帳**（発行・所有・移転をサーバーが唯一の正とする）への移行が他の何より先に必要。
- 流通は**3段階**で到達：①ゲーム内メダル市場 → ②疑似マーケット表示（相場/ランキング/来歴の可視化）→ ③本格RMT（外部マーケット/エスクロー/本人確認）。各段でリスクを潰す。
- 本格RMTは日本で重い法規リスク（賭博罪・景表法/コンプガチャ・資金決済法・古物営業法）。**換金性の置き場所を制御できる構造**（メダル経済とRMT層を分離可能）にしておく。※非弁護士の整理。

## 1. 価値の源泉（Mirage Gateへの対応）
| 価値の源泉 | 一般原則 | Mirage Gateでの担い手 |
|---|---|---|
| ① 希少性（証明可能） | 何個・何番目・同パターン何個 | サーバー発行シリアル＋ミント上限＋patternSeed分布の公開 |
| ② 見た目（一瞬で分かる） | 見た瞬間に強い | 変異オーラ＋レアパターン配色＋レア度の箔/ホロ |
| ③ ゲーム内露出 | 画面に映るほど価値UP | 同行仲間で常時表示／装備剣は攻撃モーションで毎ターン／ロビー・プロフィール |
| ④ 流動性 | 売れる・交換・相場が見える | メダル市場→疑似マーケット→RMT。検索/絞り込み/価格履歴 |
| ⑤ 運営信頼 | 再販乱発・上位互換・急仕様変更をしない | 供給ルール事前公開＋凍結コミットメント |

固有の強み：「撃破でカード化」「育成上限cap」「格上を倒すほど伸びる」という**語れる獲得文脈**が既にある＝来歴(provenance)と好相性。「★7『幻』8F・幻影のオーブ討伐時に勧誘された個体」はそれだけで物語。

## 2. 個体化の担い手
| 候補 | 露出 | 既存ループ親和 | 個体差の自然さ | 推奨 |
|---|---|---|---|---|
| モンスターカード（仲間） | 高 | charm/勧誘→monCards→召喚が既存 | 高 | ★主役 |
| 装備の剣/盾/指輪 | 中〜高 | swordAtk/drain等が既存 | 高（CS2型スキン） | ★併走 |
| 職スキン（ヒーロー） | 高 | 8方向スプライト資産 | 中（職固定で個体差薄） | ☆将来 |

第一弾＝「モンスターカード（資産の主役）」「剣スキン（見せびらかしの主役）」の二本柱。職スキンは第二弾でビジュアルスキン展開。

## 3. 個体データ構造（スキーマ）
発行時確定・不変の `core` ＋ 移転で変わる `meta`。
```jsonc
Instance {
  core: { // 発行サーバーが署名、以後改変不可
    instanceId, kind:"monster"|"weapon"|"cosmetic", baseId, rarity,
    patternSeed,            // 柄・配色の決定Seed(0–9999)
    wear,                   // 状態値 0.00(新品)–1.00(ボロ)
    mutation,               // null or 固定変異名
    statRoll:{ atk,def,crit, traits:[...] }, // PoE型ロール(モンスター)。武器はequipEffect
    mint:{ serial, mintCap, season, isFounder }, // #通し番号・発行上限
    provenance:{ dungeon, floor, sourceKill, bornAt, eventTag, devSigned },
    sig                     // coreへのサーバー署名(真贋)
  },
  meta:{ owner, ownerHistory:[...], bound, listing, favoriteMark, nickname }
}
```
既存統合：`G.monCards={type:count}` → `G.inventory=[instanceId...]`（個体実体は inventoryStore／Phase2以降サーバー）。剣は statRoll を equipEffect に置換し kind:"weapon"。

## 4. 生成ロジック（制御されたランダム）
**個体が生まれるモーメントを限定**＝供給制御の肝。撃破→勧誘／チャーム→確保／ボックス・スロット／イベント・大会（mintCap極小）。
**7ステップ**：①ベース抽選（由来文脈で重み）②レアリティ（格上/深層/イベントで上振れ）③wear（取得文脈で補正）④patternSeed（一部Seed帯=神パターン）⑤statRoll/equipEffect ⑥変異（固定・低確率）⑦ID/シリアル発行・来歴・署名。
- **④Seedと⑥変異は⑦採番前に確定・以後不変**（CS2 Case Hardened #661 構造）。
- **wear**：会心撃破-0.05／無傷確保-0.03／瀕死辛勝+0.08／深層・ボス由来は低wear。低wear×人気ベース×良パターン×変異＝最高額。
- **patternSeed→見た目**：`mulberry32(seed)`で決定的に配色。`accentArea>0.85`で中央集中=「Blue Gem」帯。Seed分布とtier境界は公開。
- **変異（固定型）**：Rainbow0.03%/Divine0.05%/Cursed0.12%/Radioactive0.20%/Galaxy0.40%/Lava0.30%。**見た目＞性能**（価値=希少性7:性能3、Pay-to-Win回避）。
- **供給コントロール**：mintCap（ベース×レア×シーズン）／Founder帯#1–1000恒久区別／イベント個体は極小cap／死亡消失等のburn圧で需要枯渇を防ぐ。

## 5. 見た目反映（見た瞬間に分かる）
レア度=箔/ホロ（既存 R<SR<SSR<UR<LR に接続）／変異=常時オーラ（最重要）／パターン=gem個体は明確に派手／wear=摩耗・色褪せ重ね／来歴バッジ=Founder王冠・eventリボン・devサイン箔。

## 6. 来歴（provenance）
ミント番号・Founder帯・発行シーズン・由来ダンジョン/フロア/討伐対象・生成日時・大会個体・開発者サイン・所有履歴。全て署名付き改変不可＝真贋証明＝高額取引（本人確認＋エスクロー）の前提。

## 7. 見せびらかし導線（露出）
同行仲間で常時画面／装備剣の攻撃モーション／プロフィール・ショーケース(3–6体)／ランキング・殿堂／観戦・リプレイ(将来)／ロビー・ギルド(将来)。露出↑＝需要↑＝流動性の燃料。

## 8. 流通・市場（本丸）
**3段階**：A. ゲーム内メダル市場（現金なし＝法務最小）→ B. 疑似マーケット表示（価格履歴/相場/ランキング/来歴/発行数）→ C. 本格RMT（外部/エスクロー/KYC/出金）。
必須機能：出品/購入/オファー(個体＋メダル混在)・検索絞り込み(base/rarity/wear/seed/mutation/serial/founder/event)・価格履歴・トレードホールド・エスクロー。
取引3形態：公式マーケット／プレイヤー間トレード／外部マーケット(Phase C)。

## 9. アーキテクチャ転換（最重要・技術判断）
現状=単一HTML＋localStorage＝自己発行し放題＝価値ゼロ。**所有の正をクライアントから剥がしサーバーへ**。
```
[クライアント] 描画/戦闘/演出は据え置き・“表示”は持つが“所有の正”は持たない
   │ REST/WS(認証必須)
[個体台帳サービス] 発行(serial採番+ed25519署名)/所有(DBが正)/移転(原子的)/真贋(公開鍵)
[マーケット] 出品/約定/価格履歴/エスクロー/手数料   [認証/KYC]   [不正対策]
```
**生成の正当性**：クライアント乱数申告は不正温床→**サーバーseed(commit-reveal)**で patternSeed/wear/mutation/serial をサーバー側確定、クライアントは演出のみ。
移行：Phase0(localStorageでも個体化＋来歴＋見た目は可・市場なし)→Phase2(台帳サーバー・市場対象個体を権威化)→Phase C(KYC/出金)。

## 10. 不正・詐欺対策（ローンチ要件・後付け不可）
複製/自己発行→サーバー採番+署名・client値は表示専用／改ざん→core署名検証／なりすまし→認証+KYC／詐欺トレード→原子的一括移転+内容固定確認／乗っ取り流出→トレードホールド+出金遅延+2FA／bot大量生成→レート制限+戦闘ログ整合+異常検知／マネロン→KYC+取引上限+監査／価格操作→同一資金循環検知+約定公開。

## 11. 供給コントロールと運営信頼（凍結コミットメント）
既存baseのmintCapは事後増やさない（増やすなら新シーズン/新baseで分離）／イベント・Founder帯は二度と再発行しない／上位互換インフレ回避（新規は別の強み・見た目で差別化）／仕様変更は予告期間。メダル経済は個体経済の“蛇口”として残し、**換金性はRMT層(Phase C)にのみ置き、メダル層と隔壁**。

## 12. 法規・リスク（日本前提・※非弁護士）
賭博罪（メダルを賭け→ランダムで換金可能個体）／景表法・コンプガチャ（組合せ収集型）／資金決済法（前払式/為替的性質）／古物営業法（中古二次流通）／RMTのToS扱い／犯収法・マネロン（KYC事実上必須）／未成年保護・確率表示。
**スタンス**：まず Phase A/B（現金化なし）で価値と人口を育てる→Phase C は「換金可能個体は射幸で得させない／マーケット許認可／KYC」を前提に**必ず弁護士レビュー後**に開く。個体に **tradableForCash** フラグを持たせ規制に応じオン/オフ（後戻り可能性を担保）。

## 13. 段階的導入ロードマップ
| Phase | 目的 | 主実装 | 法務/不正リスク | 既存影響 |
|---|---|---|---|---|
| 0 個体化体験検証 | 手応え検証 | core/metaスキーマ・生成7step・wear/pattern/mutation/serial・見た目・来歴。localStorageのまま | 低（市場なし） | 描画/データ拡張のみ |
| 1 見せびらかし | 露出 | ショーケース・同行常時表示・来歴バッジ・ランキング | 低 | UI追加 |
| 2 サーバー権威+メダル市場 | 希少性を本物に | 台帳サーバー・署名発行・メダル市場・トレード・検索/価格履歴・不正対策 | 中 | 所有層サーバー化 |
| 3 本格RMT | 現金化 | 外部マーケット・エスクロー・KYC・出金・tradableForCash制御 | 高 | 認証/決済/コンプラ |

各Phaseは独立して価値を出す。Phase2前にPhase0/1で「遊ぶ・見せたくなる」を必ず検証（人口が土台）。

## 14. 擬似コード（足場）
- `mintInstance(ctx)`：サーバー乱数→rarity/base/patternSeed/wear/mutation/statRoll/serial(cap超で停止)→core署名→ledger.create(bound)。
- `patternTier(seed)`：mulberry32でaccent算出→gem/good/normal。
- `MUTATIONS`：name/p/mult テーブル＋`rollMutation`。
- `settleTrade(offer)`：両者署名/所有/非bound検証→DBトランザクションで原子移転＋ownerHistory追記＋価格履歴。
- `searchListings({...})`：base/rarity/mutation/wear範囲/patternTier/founder/event/serial/sort。

## 15. アンチパターン回避チェックリスト（全✅必須）
□ 違いが一目で分かる（変異/gem/wear/箔） □ 供給が無限でない（cap/採番/burn） □ ゲームが面白く人口がいる □ 取引が簡単（検索/相場/低摩擦） □ 運営が価値を壊さない約束を事前公開 □ 露出面がある □ 真贋証明できる（署名/来歴） □ 性能インフレでP2W化しない（希少性7:性能3） □（Phase C）法務/KYC/換金フラグ制御。

## 16. まとめ
既存ループ（撃破→勧誘→召喚）と育成上限思想は本経済と好相性。**最大の分岐点は「本格RMTを目指すならサーバー権威台帳＋日本の法規クリアが不可避」**。進め方：Phase0で手応え・人口→Phase2で希少性を本物に→Phase Cは法務クリア後。tradableForCashで常に安全側に倒せる構造に。

---

## 実装状況（このリポジトリ）
- **Phase 0 を `monster_gate.html` に実装**（2026-06-20）。詳細は本体コードの「個体化(Instance) Phase0」ブロックと、ヘッダの「個体コレクション」ボタン参照。
  - スキーマ（core/meta・tradableForCash:false）／生成7ステップ（mulberry32・rollRarity・rollWear・patternTier・rollMutation・ローカルserial採番）。
  - 勧誘(`tryRecruit`)成功時に個体をミントして `G.inv` に追加（既存 `G.monCards` は召喚互換のため併存）。
  - 見た目反映：レア度枠／変異オーラ／gemパターン配色／wearバー／シリアル#・Founder・来歴バッジを「個体コレクション」モーダルで表示。
  - localStorage に `inv` を保存（**Phase0は改ざん対策なし＝市場・換金は未接続**。これは設計どおりの体験検証用）。
- Phase 1（露出強化：同行個体のオーラ表示・ショーケース）以降、Phase 2（サーバー台帳）／Phase 3（RMT・法務）は未実装＝今後。

---

## 17. PvP城取り・シーズン・収入・進化（2026-06-21 追記）

### いま実装済み（クライアント単体）
- **ブレンド（進化）`evoLevel`**：同じモンスター×2 → 一定確率で進化Lv+1（statRoll ×1.25/Lv）。**レア度とは別概念で複合**。レベルUP時ごく稀に変異発現・レア度UP。失敗で1枚消費。`blendGroups`/`doBlend`、個体コレクションの「🧪ブレンド」から。価格/防衛力にも反映。
- **③復活時レア度UP**／**④ワールドボスのカード化（変異/特殊カラー/HF原画）**／**⑦城ガーディアン（個体statRoll/進化Lvで防衛力）＋worldDefensePhaseでの侵略奪還** は実装済み。

### 将来＝サーバー権威必須（オフラインHTMLでは“本物”にできない）
本節は §9（サーバー台帳）の上に乗る本格PvP設計。実装はバックエンド前提。
1. **PvP城取り合い**：城の所有・占領・奪還を全プレイヤーで共有（サーバーDBが唯一の正）。攻撃＝相手の城へ侵攻し、配置ガーディアン（ボス）を攻略すれば占領。クライアント改ざん対策（戦闘ログ検証・レート制限）必須。
2. **シーズン制＆報酬**：占領城数 × 城レベル × 占領率 でランキング/報酬。シーズン終了で集計・配布・一部リセット。
3. **PvP専用ワールドモード**：通常(PvE)ワールドと分離した対人マップ。
4. **ガーディアン最大5体＝ボス**：1城に最大5体配置。攻撃側の前にボスとして順に立ちはだかる。**全滅させられたガーディアンは消滅（inv から burn）**。配置すると攻略難易度が大幅UP。
5. **城の定期収入**：占領中の城が秒単位で報酬を蓄積（城レベル×占領率でレート可変）。**1日1回回収**。**収入特性を持つモンスター**を配置すると額UP。**占領（奪取）されると貯まった報酬ごと奪われる**。配置ガーディアンは侵略で倒される/奪還されると消滅。
6. **進化×変異×レア度の三軸**：evoLevel（進化）／mutation（変異）／rarity（レア度）が独立に積み上がり、PvP配置・市場価値・防衛力に複合。

### 段階移行（§13ロードマップに接続）
- Phase 2（サーバー台帳）で①所有/占領/収入の権威化 → Phase 3（PvP）で対人侵攻・シーズン・報酬配布・KYC/換金。
- クライアント側は既に evoLevel / guardianStrength / worldDefensePhase / income特性フラグの素地があるので、サーバーAPIに載せ替える形で拡張可能。
- ④⑤（5体ガーディアン・収入）は**ローカル単体版（対AI・自分の占領地）**としても先行実装可能（PvPなしで体験検証）。
